version: "3.9"

services:
  beerfinder:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./beer_finder.db:/app/beer_finder.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
