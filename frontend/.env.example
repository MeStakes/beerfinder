# URL del backend FastAPI (Railway).
# In locale lascialo vuoto: il dev server Vite fa da proxy di /api verso http://localhost:8000.
# In produzione (Vercel) impostalo all'URL pubblico del backend, es.:
#   VITE_API_URL=https://beerfinder-production.up.railway.app
VITE_API_URL=
